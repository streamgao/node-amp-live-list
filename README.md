# node-amp-live-list-test

based on:  https://github.com/eh3rrera/node-amp-live-list/

support endpoints:  
- GET /new
- GET /delete
- GET /update


run `node server.js`
and open browser at: `localhost:3000/`
